post:
  tags:
    - Auth
  summary: Request password reset code
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../schemas/ForgotPasswordRequest.yaml"
  responses:
    "200":
      description: Generic success response
      content:
        application/json:
          schema:
            $ref: "../schemas/SuccessEnvelope.yaml"

